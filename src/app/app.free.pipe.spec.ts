import { TestBed, async } from '@angular/core/testing';
import { FreePipe } from './app.free.pipe';

describe('FreePipe', () => {
  it('create an instance', () => {
    const pipe = new FreePipe();
    expect(pipe).toBeTruthy();
  });
});